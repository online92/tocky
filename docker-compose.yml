services:
  tocky:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_GOOGLE_API_KEY: ${VITE_GOOGLE_API_KEY}
    environment:
      - VITE_GOOGLE_API_KEY=${VITE_GOOGLE_API_KEY}
    ports:
      - "8080:80"
